# Rose — EOD Log

## 2026-04-23 — Chat migration wraps, /research built, repo cleanup

**Done:**
- Shipped transparent-on-scroll nav on v2 homepage (plum past 80px, transparent over hero)
- Replaced chat.html nav with homepage-style .rose-nav; always plum, wordmark links back to askrose.io
- Added /index.html redirect to rose-website vercel.json so the old marketing homepage stops serving at chat.askrose.io
- Removed unreachable marketing orphans from rose-website (index.html, favicon-test.png, rose-v17b.zip)
- Deleted duplicate root-level system-prompt.txt; canonical copy lives at /rose/system-prompt.txt (untracked, disk-only cleanup)
- Removed unused ChatInterface component + orphaned setUserProfile state from v2 (187 lines)
- Added .gitignore to v2 and committed pnpm-lock.yaml so Vercel builds from pinned versions
- Built /research page: new ResearchEntry component + 19 scholar and institutional entries across 5 categories, replacing the "Coming soon" stub

**Commits:**
- `3fb8147` — transparent-on-scroll nav on v2 homepage
- `3579441` — /index.html redirect in rose-website vercel.json
- `43f63f5` — chat.html nav replaced with .rose-nav structure
- `01dcc22` — ChatInterface removed from v2
- `36b14fc` — .gitignore + lockfile added to v2
- (pending) — /research page build, not yet pushed

**State at EOD:**
Homepage, chat, and /research now read as one continuous brand on the rose/blush/midnight palette. Dead code pruned across both repos. /research is locally built and reviewed but the commit is still pending Kim's push.

**Open / next:**
- Push the /research commit (ResearchEntry + Research.tsx)
- Shared chat-style header component: deferred from the research brief, needed whenever Kim decides to add a proper header to /research
- styles.css in rose-website still has marketing-only selectors; flagged as a separate surgical task
- Low-noise follow-ups: loosen package.json engines.node pin, run pnpm approve-builds for esbuild/tailwindcss-oxide, bump @types/node to >=18
- Source of truth for research entries: Rose_Research_Sources.xlsx. Future additions go to sheet first, then port to page.
